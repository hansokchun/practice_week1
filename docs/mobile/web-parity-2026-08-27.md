# 웹 개편 모바일 동기화 원장

기준일: 2026-08-30
웹 기준: 2026-08-30 `dev` 랜딩·검색·태그 갤러리·로그인·사진 상세·Explore 개편

## 확인한 웹 변경

- 로그인 여부와 관계없이 관리형 공개 사진 랜딩을 기본 화면으로 사용한다.
- 상단·고정 하단 탐색을 제거하고 로고, `사진 추가`, 로그인 또는 계정 메뉴만 둔다.
- 큰 공개 사진 검색, `제주 바다·서울 야경·일본` 추천 검색어, `추천·한국·일본·풍경·도시` 가로 사진 섹션과 두 곳의 지도 진입을 제공한다.
- 웹의 태그 전체보기는 지도 없이 5열·30장 페이지와 지역 필터를 제공한다.
- 계정 메뉴는 `내 프로필`, `내 사진`, `좋아요한 사진`으로 제한한다.
- `내 사진` 안에서 사진과 웹 앨범을 전환한다.
- 사진 상세에서 주변 사진 목록을 제거하고, 정확 위치에서만 요청 후 거리뷰를 연다.
- 랜딩 제목은 `이끼에서 당신만의 장소를 찾아보세요`를 사용한다. 지도 진입은 큰 `지도에서 찾아보기` 버튼 하나로 정리하고, 하단 지도·핀 이미지는 문구나 카드 경계 없이 페이지 배경으로 페이드한다.
- 로그인 화면에서 소개·신규 계정 제한·개발자 안내 문구를 제거한다.
- 사진 상세는 하트만 있는 좋아요 버튼, `-- --` 날짜 대체, 스크롤 단서, 제목 높이의 메뉴, 소유자 전용 공개 상태, 작동하는 Explore 지도 이동을 사용한다.
- Explore의 `내 사진`은 공개 여부와 무관하게 소유자가 가진 위치 사진을 표시하고, `다른 사람 사진`은 공개 위치 정책을 유지한다.
- `app_metadata.role = admin`인 웹 운영자는 랜딩 섹션과 사진 순서를 관리한다.

## 모바일 반영 상태

| 변경 | 상태 | 모바일 근거 |
| --- | --- | --- |
| 공개 사진 랜딩을 첫 화면으로 사용 | 완료 | `mobile/src/LandingScreen.tsx`, `mobile/app/(tabs)/index.tsx` |
| 고정 하단 탭 제거 | 완료 | 숨김 라우트 등록만 유지하고 `tabBarStyle.display = none` 적용 |
| 사진 추가·로그인·계정 메뉴 | 완료 | `/upload`, `/profile`, `/my-photos`, `/likes` 이동 |
| 웹 관리형 섹션 공유 | 완료 | `landing_sections`, `landing_section_photos`, `photos`를 같은 운영 Supabase에서 읽음 |
| 큰 검색·추천어·가로 사진 목록 | 완료 | 공개 사진의 제목·설명·앨범 검색과 수평 스크롤 |
| 추천 검색어 3개 통일 | 완료 | `제주 바다·서울 야경·일본`만 표시하고 회귀 테스트로 고정 |
| 태그 전체보기 | 첫 모바일 베타 이후 | 첫 모바일 베타는 랜딩 가로 목록과 검색·Explore에 집중하고 별도 태그 갤러리 라우트는 웹 전용으로 유지 |
| 지도 진입 분리 | 완료 | `/explore`에서 기존 네이티브 지도·Places 검색 유지 |
| 공개 사진 상세 연결 | 완료 | 기존 `/explore-photo/[photoId]` 재사용; 주변 사진 UI는 원래 없음 |
| 최신 랜딩 문구·지도 배경 CTA | 완료 | 웹의 지도 핀 배경 자산을 모바일 CTA에 재사용 |
| 단순 로그인 화면 | 완료 | 이메일·Google·Kakao만 유지하고 내부 QA 문구 제거 |
| 상세 좋아요·날짜·메뉴·스크롤 단서 | 완료 | 하트 전용 버튼, reduced-motion 대응 모션, `-- --`, 제목 메뉴, 이미지 하단 스크롤 단서 |
| 상세에서 Explore 지도 이동 | 완료 | 공개 가능한 좌표만 라우트 파라미터로 전달하고 해당 사진·범위에 초점 |
| 내 사진 전체 위치 범위 | 완료 | owner-only `photo_private_locations`를 읽어 비공개·공개 사진을 소유자 지도에만 표시 |
| 로그인 사용자 첫 지도 시점 | 완료 | 소유자 전용 위치 전체 bounds에 여유를 더해 첫 카메라 구성 |
| 군집 확대·분해 | 완료 | 군집 원본 좌표 bounds에 여유를 둔 380ms 네이티브 카메라 애니메이션 |
| 서명 이미지 만료·foreground 갱신 | 완료 | 270초 재조회와 공통 foreground refresh key 적용 |
| 웹 앨범을 내 사진 안에 표시 | 미완료 | 기존 모바일의 앨범 제외 경계를 새 제품 결정에 맞춰 교체해야 함 |
| 정확 위치 거리뷰 | 부분 완료 | exact만 Google Maps panorama URL로 이동; 외부 앱 왕복은 실기기 검증 필요 |
| 랜딩 관리자 편집 화면 | 웹 운영 전용 | 앱은 RLS로 공개된 결과만 읽으며 관리자 쓰기는 기존 웹 운영 화면에 유지 |
| iOS·Android 실제 화면 QA | 미완료 | 가로 스와이프, 안전 영역, 이미지 만료, 지도 복귀를 서명 실기기에서 확인 |

## 운영 데이터 읽기 확인

2026-08-27 운영 publishable key의 익명 역할로 읽기만 수행했다.

- 공개 랜딩 섹션: 5개 (`추천`, `한국`, `일본`, `풍경`, `도시`)
- 관리자 사진 배정: 0개
- 랜딩 조회 가능한 공개 사진: 2개
- 공개 사진의 Storage 경로 형식: 모두 안전한 소유자 하위 경로

사진 배정이 없는 현재 운영 상태에서는 웹과 모바일 모두 공개 사진을 섹션별로 순환 배치한다. 관리자 배정이 저장되면 앱은 같은 테이블의 순서대로 즉시 표시한다.

## 다음 구현 순서

1. 서명 실기기 QA로 이번 웹·앱 동작 일치를 승인한다. 자동 회귀와 production export는 2026-08-30 통과했다.
2. exact 거리뷰 외부 앱 왕복과 Explore 초점 복귀를 iOS·Android에서 확인한다.
3. 첫 베타 이후 웹 `albums`·`album_photos` 계약과 모바일 로컬 사진 경계를 충돌 없이 결합한다.
4. AI 앨범은 분석 결과 계약과 사용자 확인 단계를 먼저 정의한 뒤 별도 기능으로 추가한다.
